<h1>All Tasks</h1>
<%= link_to "Create a new task", new_task_path%>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <%= task.completed ? "✅" : "❎" %></li>
    <li><%= task.details %></li>
 <% end %>
</ul>

